<!--
No connector URL, no internal hostname, no absolute path off your machine,
nobody else's name. Redact rather than omit: "HTTP 401 from the configured
endpoint" carries what the value would. This title and body are scanned by
.github/workflows/pr-text.yml, and a red check there means revoke the
credential in relay before anything else.

Why each heading is here:
[Pull requests](../docs/contributing/development.md#pull-requests).

Delete a heading that has nothing under it, and delete this comment.
-->

## What changed, and why

The problem, not just the edit.

## Anything deleted, and why it was safe

Name what replaced it.

## Behaviour changes for existing users

The error they will see, and what to do about it.

## Verification

Which tests, on what, and anything checked by hand. Say plainly if something is
unverified. Do not claim a check you did not run.

## What was left out

What you decided not to do here, so a reviewer does not have to guess.
